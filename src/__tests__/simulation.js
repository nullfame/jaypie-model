// eslint-disable-next-line no-unused-vars
class AnimalModel {
  // constructor(params) {}
}

// eslint-disable-next-line no-unused-vars
function animalController(event) {
  //
}

// eslint-disable-next-line import/prefer-default-export
export const animalHandler = event => animalController(event);
